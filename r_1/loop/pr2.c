#include <stdio.h>
main()
{
    int f=0 ,s=1,num,ans;

    printf("Enter Number:");
    scanf("%d",&num);

    for(int i=0; i<=n; i++)
    {
        printf("%d\n",f);
        ans=f+s;
        f=s;
        s=ans;
    }

}